<h2>SHOW</h2>
<%= attachment_image_tag @blog, :image, :fill,400,400, format: "jpeg" %>
<h3><%= @blog.title %></h3>
<p><%= @blog.created_at %></p>
<p><%= @blog.body %></p>
 <%= link_to "Edit", edit_blog_path(@blog) %> | 
<%= link_to "Delete", blog_path(@blog), method: :delete, "data-confirm" => "Are you sure you want to delete this post?"%>
 
 
